STORE "KODE_BARANG" AS INTERGER WITH ANY VALUE
STORE "Jumlah" AS INTERGER WITH ANY VALUE
STORE "Harga" AS INTERGER WITH ANY VALUE
STORE "Total" AS INTERGER WITH 0
STORE "TOTAL" AS INTERGER WITH 0

STORE "Total" WITH "Jumlah" TIMES "Harga"
DISPLAY "Total"

IF "KODE_BARANG" ADDED "JUMLAH"
    DISPLAY "Harga"
    STORE "TOTAL" WITH "Total" Times "0.8"
    DISPLAY "TOTAL"

ELSE "KODE_BARANG" ACCORDANCE WITH "Jumlah"
    "DISPLAY "Harga"
    STORE "TOTAL" WITH "Total" Times "0.8"
    DISPLAY "TOTAL"

END IF